#include "constants.hpp"

class Position {
  private:
    int row;
    int col;

  public:
    toNotation()
};

class Piece {
  private:
      Color color;
      Position position;
      Type type;
      std::string symbol;
};
